// this way we mock NPM MODULES so when we are in test mode we don waste rela resources
// in this case to not send emails from sengrid

module.exports = {
    setApiKey() {

    },
    send() {

    }
};